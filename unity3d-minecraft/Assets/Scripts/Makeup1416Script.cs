using UnityEngine;

public class Makeup1416Script : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log(GameObject.Find("Cube"));
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
